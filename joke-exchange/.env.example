# Optional AI moderation gate (Wix AI Gateway / Prompt Hub).
# Leave off — keyword heuristics in src/lib/classify.ts run instead.
AI_MODERATION=off

# Prompt Hub prompt id used only when AI_MODERATION=on
PROMPT_HUB_PROMPT_ID=YOUR_PROMPT_ID_HERE
