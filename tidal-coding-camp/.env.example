# Copy this file to `.env.local` and fill in your values.
# Never commit a real `.env.local`.

# Guards GET /api/setup-seats?token=… (one-time CMS seat-collection bootstrap).
# Pick any random string; do not reuse a production secret.
SETUP_TOKEN=YOUR_SETUP_TOKEN_HERE
