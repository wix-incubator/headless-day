# Optional — powers the AI concierge chat on the site (/api/concierge).
# Set in production via: wix env set --key ANTHROPIC_API_KEY --value <key>
# Then sync locally with: wix env pull
ANTHROPIC_API_KEY=YOUR_ANTHROPIC_API_KEY_HERE
